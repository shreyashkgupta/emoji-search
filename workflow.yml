---
# Auto-generated by Cloud Code AI
name: CI
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker image
        run: docker build . --file Dockerfile -t myapp
      - name: Test the Docker image
        run: docker run --name test myapp python3 -m unittest
      - name: Deploy to Docker Hub
        run: |-
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker tag myapp ${{ secrets.DOCKER_USERNAME }}/myapp
          docker push ${{ secrets.DOCKER_USERNAME }}/myapp
